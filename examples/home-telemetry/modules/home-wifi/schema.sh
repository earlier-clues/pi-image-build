# home-wifi — install a single NetworkManager wifi profile from env.
# Payload-local. If a second payload ever needs this, promote it to a
# repo-level module under modules/wifi-profile/.

require WIFI_SSID
require WIFI_PSK
